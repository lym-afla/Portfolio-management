[tool.black]
line-length = 100
target-version = ['py310']
include = '\.pyi?$'
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | \venv
  | venv
  | _build
  | buck-out
  | build
  | dist
  | migrations
)/
'''

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "portfolio_management.settings"
python_files = ["test_*.py", "tests.py", "*_tests.py"]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = [
    "--tb=short",
    "-ra",
    "--strict-markers",
    "--strict-config"
]

# Custom pytest markers for test categorization
markers = [
    "django_db: Mark the test as needing the database",
    "asyncio: Mark the test as async",
    "unit: marks tests as unit tests",
    "integration: marks tests as integration tests",
    "performance: marks tests as performance tests",
    "regression: marks tests as regression tests",
    "edge_case: marks tests as edge case tests",
    "production: marks tests as production snapshot tests",
    "nav: marks tests related to NAV calculations",
    "fx: marks tests related to FX calculations",
    "buy_in_price: marks tests related to buy-in price calculations",
    "gain_loss: marks tests related to gain/loss calculations",
]
