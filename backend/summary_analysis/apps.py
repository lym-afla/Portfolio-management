"""Django app configuration for summary_analysis."""

from django.apps import AppConfig


class SummaryAnalysisConfig(AppConfig):
    """Django configuration for the summary_analysis app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "summary_analysis"
