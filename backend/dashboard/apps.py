"""Django app configuration for dashboard."""

from django.apps import AppConfig


class DashboardConfig(AppConfig):
    """Django configuration for the dashboard app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dashboard"
