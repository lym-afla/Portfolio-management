"""Django app configuration for open_positions."""

from django.apps import AppConfig


class OpenPositionsConfig(AppConfig):
    """Django configuration for the open_positions app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "open_positions"
